<div>
    <h1 class="titulo">Lista de productos</h1>

    <table class="listaProds">
        <tr>
            <th>Título</th>
            <th>Precio</th>
            <th>Miniatura</th>
        </tr>
        {{#if productos.length}}
        {{#each productos}}
        <tr>
            <td>{{this.title}}</td>
            <td>{{this.price}}</td>
            <td><img height="60px" width="60px" src={{this.thumbnail}} /></td>
        </tr>
        {{/each}}
        {{/if}}
        {{#unless}}
        <h1>producto no encontrado</h1>
        {{/unless}}
    </table>

    <a href="/"><button>Volver al formulario productos</button></a>

</div>

<style>
    table, th, td {
        border: 0.5px solid;
    }

    .titulo {
        text-align: center;
    }

    .listaProds{
        width: 100%;
    }
</style>